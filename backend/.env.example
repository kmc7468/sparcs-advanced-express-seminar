# General
PORT=8000

# Database
DATABASE_URL=mysql://root:root@localhost:3306/gallery   # Required
MAX_UPLOAD_SIZE_MB=10

# Security
JWT_SECRET=replace-with-long-random-secret              # Required
JWT_EXPIRES_IN=7d
COOKIE_NAME=auth
ALLOWED_ORIGINS=http://localhost:5173

# AWS
AWS_REGION=ap-northeast-2                               # Required
AWS_S3_BUCKET=sparcs-web-seminar                        # Required
AWS_ACCESS_KEY_ID=                                      # Required
AWS_SECRET_ACCESS_KEY=                                  # Required
AWS_CLOUDFRONT_URL=https://d5il24m7xaef1.cloudfront.net
